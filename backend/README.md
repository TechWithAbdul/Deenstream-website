DeenStream AI - backend scaffold

This backend skeleton uses FastAPI. It provides modular routers for quran, hadith, prayer, and AI integration.

Run locally:
- python -m venv .venv
- .\\.venv\\Scripts\\activate
- pip install -r requirements.txt
- uvicorn app.main:app --reload
